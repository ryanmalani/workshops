kind: ZarfPackageConfig
metadata:
  name: "svelte-ui"
  description: "Svelte UI for Workshop"
  version: "0.0.1"

components:
  # Uncomment the below component to use it
  # This will package the defined manifests and images
  # we want for this application at deployment

  # - name: ui
  #   required: true
  #   manifests:
  #     - name: ui
  #       namespace: svelte
  #       files:
  #         - manifests/uds-package.yaml
  #         - manifests/namespace.yaml
  #         - manifests/service.yaml
  #         - manifests/deployment.yaml
  #   images:
  #     - ghcr.io/defenseunicorns/workshops/ui:0.0.1